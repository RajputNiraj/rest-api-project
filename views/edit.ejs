<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Book</title>
  </head>
  <body>
    <h1>Edit Page</h1>
    <h3>Edit Book</h3>

    <%# The form still sends a POST request to Express, but the middleware
    methodOverride changes the method to PUT before Express receives the
    request. #%>

    <form action="/edit/<%= book.id %>?_method=PUT" method="POST">
      <input type="hidden" name="_method" value="PUT" />
      <div>
        <label for="title">Title:</label>
        <input
          type="text"
          id="title"
          name="title"
          value="<%= book.title %>"
          required
        />
      </div>
      <div>
        <label for="author">Author:</label>
        <input
          type="text"
          id="author"
          name="author"
          value="<%= book.author %>"
          required
        />
      </div>
      <div>
        <label for="available">Available:</label>
        <input type="checkbox" id="available" name="available" <%=
        book.available ? 'checked' : '' %> />
      </div>
      <button type="submit">Update Book</button>
    </form>
  </body>
</html>
